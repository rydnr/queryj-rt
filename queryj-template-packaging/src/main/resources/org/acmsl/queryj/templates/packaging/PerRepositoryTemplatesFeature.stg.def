/*
 * PerRepositoryTemplatesFeature template metadata.
 */
// The template name (the filename).
name : PerRepositoryTemplatesFeature.stg;

// The type of template: per-table, per-repository, per-foreign-key, per-custom-result, per-sql.
type : per-repository;

// The output flavor.
output: Cucumber;

// The filename builder.
filename  builder : PerForeignKeyTemplates.feature;

// The package.
package: cucumber.templates;

disabled;