/*
 * PerForeignKeyTemplateWritingHandler template metadata.
 */
// The template name (the filename).
name : PerForeignKeyTemplateWritingHandler.stg;

// The type of template: per-table, per-repository, per-foreign-key, per-custom-result, per-sql.
type : per-foreign-key;

// The output flavor.
output: Java;

// The filename builder.
filename  builder : PerTableTemplates.feature;

// The package.
package: cucumber.templates;

disabled;
